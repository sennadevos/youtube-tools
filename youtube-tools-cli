#!/usr/bin/env python3
"""
YouTube Tools CLI Entry Point
Direct CLI access without launching GUI.
"""

import sys
import os

# Add the current directory to Python path so we can import youtube_tools
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from youtube_tools.cli import main

if __name__ == "__main__":
    main()